### Example environment variables
# Set this to true to use the built-in mock responses instead of real network calls
VITE_USE_MOCK=true
VITE_DEV_MODE=true
VITE_API_BASE=https://api.example.com

# When using real API, set an API base (not used by mock)
